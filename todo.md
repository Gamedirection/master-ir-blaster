Windows / Apple builds

Remote Widgets for KDE Desktop

Discord integrations

Teams webhook integration (Power Automate): paste an incoming-webhook URL from
a Teams workflow into the app. A ping triggers a defined light sequence
(e.g. Flash Red -> wait X sec -> go Red -> confirm complete -> Green).
A sequence with a defined end state overrides schedule/Teams-status
changes until it finishes; otherwise those keep working normally.


